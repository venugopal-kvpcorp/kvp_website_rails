<%= render 'refinery/admin/search_header', :url => refinery.media_admin_media_path %>
<div class='pagination_container'>
  <% if @media.any? %>
    <%= render 'media' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
