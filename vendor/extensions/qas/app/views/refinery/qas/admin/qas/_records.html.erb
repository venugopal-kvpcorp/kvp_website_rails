<%= render 'refinery/admin/search_header', :url => refinery.qas_admin_qas_path %>
<div class='pagination_container'>
  <% if @qas.any? %>
    <%= render 'qas' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
