<%= render 'refinery/admin/search_header', :url => refinery.press_releases_admin_press_releases_path %>
<div class='pagination_container'>
  <% if @press_releases.any? %>
    <%= render 'press_releases' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
