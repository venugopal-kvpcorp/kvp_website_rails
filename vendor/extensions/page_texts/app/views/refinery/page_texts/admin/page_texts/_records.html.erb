<%= render 'refinery/admin/search_header', :url => refinery.page_texts_admin_page_texts_path %>
<div class='pagination_container'>
  <% if @page_texts.any? %>
    <%= render 'page_texts' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
