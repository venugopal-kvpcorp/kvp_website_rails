<ul id='sortable_list'>
  <%= render :partial => 'page_text', :collection => @page_texts %>
</ul>
<%= render '/refinery/admin/sortable_list',
            :continue_reordering => (local_assigns.keys.include?(:continue_reordering)) ? continue_reordering : true %>
